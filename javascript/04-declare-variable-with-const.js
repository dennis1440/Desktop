// 1. variable declared with const be assigned to a value
const home-address = "No MM way";

// 2. Variables declared with const should not be re-assigned to a new value
// Variables declared with const are meant to stay the same
/* the code below here is wrong, we tried to re-assign home_address to a new value
*** home-address = "No 14 MM way"; *** this is wrong
*/

console.log(home-addrss);

